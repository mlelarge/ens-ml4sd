---
layout: page
title: "Math Test"
---

# Math Rendering Test

This is a test page to verify MathJax is working properly.

## Inline Math
Here is some inline math: $x^2 + y^2 = z^2$ and another: $\alpha + \beta = \gamma$.

## Display Math
Here is a display equation:

$$
\frac{\partial L}{\partial w_i} = \sum_{j=1}^{n} (h_\theta(x^{(j)}) - y^{(j)}) x_i^{(j)}
$$

## Complex Math
Let's test subscripts and superscripts:

$$
R_S[f] = \frac{1}{n}\sum_{i=1}^{n} \text{loss}(f(x_i), y_i)
$$

If you can see the math properly formatted (not just raw LaTeX), then MathJax is working!